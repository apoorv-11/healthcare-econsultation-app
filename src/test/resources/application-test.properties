# H2 In-Memory DB Configuration for Testing
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=root
spring.datasource.password=T3rM1nAt0r_mysql@23csu042

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop  # use "create-drop" for clean state each time
spring.jpa.show-sql=true

# H2 Console (optional for debugging)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

#jwt properties
app.jwt.secret=app.jwt.secret
app.jwt.expiration-ms=86400000 # 1 day in milliseconds
